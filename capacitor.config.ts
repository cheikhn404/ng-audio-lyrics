import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ng-audio-lyrics',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
